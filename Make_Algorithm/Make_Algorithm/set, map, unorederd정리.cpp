
/*

set, map
tree 구조
시간 복잡도 O(logN)
< operator 를 정의해서 엘리멘트들이 정렬이됨.

unorederd_set, unordered_map 
내부적으로 hash structure 를 가지고
덕분에 시간 복잡도가 O(1) 이다

not sorted
O(n) -> rehashing이 필요함



set, unordered_set
엘리멘트 자체가 key가 되기때문에 key만 고려하면 된다.

map, unorderd_map
key와 value 모두 필요함


둘 모두 중복이 없다.
앞에 multi를 붙이면 중복을 허용하게 된다.


*/